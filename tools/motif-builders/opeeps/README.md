# Open Peeps motif builder

Builds a small set of “human” SVG symbols from `@opeepsfun/open-peeps` and writes them into:

- `front-end-effect/assets/motifs/people-opeeps.defs.svg`

Run:

```bash
cd front-end-effect/tools/motif-builders/opeeps
npm install
npm run build
```

